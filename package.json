{
  "name": "grant-analyst",
  "version": "1.0.0",
  "private": true,
  "description": "Evidence-first grant proposal due diligence, with a static frontend and a Render/Neon/OpenAI backend.",
  "engines": {
    "node": ">=22.13.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "build:ftp": "npm run build --workspace frontend",
    "test": "npm test --workspace backend && npm test --workspace frontend",
    "test:live": "node --test tests/live-smoke.test.mjs",
    "test:paid-live": "node --test tests/paid-analysis.test.mjs",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "migrate": "npm run migrate --workspace backend"
  },
  "license": "MIT"
}
